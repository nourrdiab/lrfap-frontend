import { PlaceholderPage } from '../../components/PlaceholderPage';

export default function UniversityRankingPage() {
  return (
    <PlaceholderPage
      title="Program ranking"
      subtitle="Drag-and-drop ranking interface with scores/notes per applicant."
    />
  );
}
