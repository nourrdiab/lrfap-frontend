import { PlaceholderPage } from '../../components/PlaceholderPage';

export default function UniversityDashboardPage() {
  return (
    <PlaceholderPage
      title="University dashboard"
      subtitle="My programs, pending reviews, pending rankings, cycle status."
    />
  );
}
