import { PlaceholderPage } from '../../components/PlaceholderPage';

export default function AboutPage() {
  return (
    <PlaceholderPage
      title="About"
      subtitle="About / Policies / Help / Contact — one page with tabs. Pending Figma frame."
    />
  );
}
