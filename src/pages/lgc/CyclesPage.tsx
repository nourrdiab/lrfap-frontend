import { PlaceholderPage } from '../../components/PlaceholderPage';

export default function LGCCyclesPage() {
  return (
    <PlaceholderPage
      title="Cycles"
      subtitle="Create/edit cycles, change status across the 7-state enum."
    />
  );
}
