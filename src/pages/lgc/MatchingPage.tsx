import { PlaceholderPage } from '../../components/PlaceholderPage';

export default function LGCMatchingPage() {
  return (
    <PlaceholderPage
      title="Matching engine"
      subtitle="Dry run / official run / past runs / publish results."
    />
  );
}
